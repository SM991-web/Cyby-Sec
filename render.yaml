services:
  - type: web
    name: cyber-chatbot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: GITHUB_TOKEN
        value: ${GITHUB_TOKEN}
      - key: OPENAI_API_KEY
        value: ${OPENAI_API_KEY}
